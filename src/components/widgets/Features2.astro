---
import { Icon } from 'astro-icon';

interface Item {
  title?: string;
  description?: string;
  icon?: string;
}

export interface Props {
  title?: string;
  subtitle?: string;
  highlight?: string;
  items: Array<Item>;
}

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  highlight,
  items = [],
} = Astro.props;
---

<section class="bg-gray-200 dark:bg-slate-750">
  <div class="max-w-7xl mx-auto px-4">
    <div class="py-4 pt-8 sm:py-6 lg:py-8 lg:pt-12">
      {
        (title || subtitle || highlight) && (
          <div class="mb-8 md:mx-auto text-center max-w-3xl">
            {highlight && (
              <p
                class="text-primary-700 dark:text-primary-400 font-semibold tracking-wide text-lg uppercase"
                set:html={highlight}
              />
            )}
            {title && (
              <h2
                class="text-4xl md:text-6xl font-black mb-4 font-serif"
                set:html={title}
              />
            )}

            {subtitle && (
              <p
                class="max-w-3xl mx-auto sm:text-center md:text-2xl text-xl text-gray-800 dark:text-slate-200"
                set:html={subtitle}
              />
            )}
          </div>
        )
      }
      <div class={`grid gap-6 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 my-12 dark:text-white items-stretch`}>
        {
          items.map(({ title, description, icon }) => (
            <div class="relative flex flex-col p-6 bg-white dark:bg-slate-900 rounded shadow-lg hover:shadow-md transition border border-transparent dark:border-slate-800">
              <div class="flex items-center">
                <Icon name={icon} class="w-10 h-10" />
                <div class="ml-4 text-xl font-bold">{title}</div>
              </div>
              {description && <p class="text-gray-600 dark:text-gray-400 text-md mt-4" set:html={description} />}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
